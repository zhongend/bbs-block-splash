# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties（与原版 2.0.4 构建清单一致：MC 1.20.1 / Loader 0.16.14 / Loom 1.15.x）
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.16.14

# Fabric API
fabric_version=0.92.1+1.20.1

# Mod Properties
mod_version=2.1.0
maven_group=com.example
archives_base_name=bbs-Block_Splash

# 前置 BBS 模组（先在 bbs-fs 1.20.1 分支执行 `gradlew publishToMavenLocal`）
bbs_version=2.5.2-1.20.1

# Jolt Physics（BBS 物理引擎 bbs-physics-engine 的同源引擎），经 stephengold 的 jolt-jni 绑定。
# ReleaseSp = 单精度构建；随 mod jar 以 jar-in-jar 方式分发。
jolt_version=1.0.0
